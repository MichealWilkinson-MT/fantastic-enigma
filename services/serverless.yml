service: fantastic-enigma
frameworkVersion: "2"

provider:
  name: aws
  runtime: go1.x
  lambdaHashingVersion: 20201221

  # you can overwrite defaults here
  stage: dev
  region: eu-west-2

package:
  patterns:
    - "!./**"
    - ./bin/**
